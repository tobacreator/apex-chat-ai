# DISABLED: This workflow has been combined with deploy.yml
# Use the "Build and Deploy" workflow instead
name: CI (DISABLED)

on:
  # Disabled to avoid conflicts with combined workflow
  workflow_dispatch: # Only manual trigger, no automatic triggers

env:
  NODE_VERSION: '18'
  NPM_CONFIG_CACHE: '~/.npm'

jobs:
  # This workflow is disabled - use deploy.yml instead
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow has been disabled."
          echo "Use the 'Build and Deploy' workflow in deploy.yml instead."
          exit 1
